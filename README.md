# WorkTwin

## Run
1. Copy `.env.example` to `.env` and add your Firebase values.
2. `npm install`
3. `npx expo start`

## Included
- Auth with email/password + guest
- Tasks with local/cloud storage
- Custom focus timer
- Statistics tab
- Theme toggle
